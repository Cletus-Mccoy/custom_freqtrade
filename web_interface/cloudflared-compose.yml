version: '3.8'
services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel --url http://web_interface:5000
    restart: unless-stopped
    networks:
      - cloudflarednet
networks:
  cloudflarednet:
    driver: bridge
